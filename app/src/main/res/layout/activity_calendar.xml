<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/chat_background"
    >
    tools:context=".CalendarActivity">
    <com.applandeo.materialcalendarview.CalendarView
        android:id="@+id/datePickCalendar"
        app:datePicker="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:headerColor="@color/colorPrimaryDark"
        app:type="many_days_picker"
        app:eventsEnabled="true"
        app:selectionColor="@color/colorPrimary"
        app:todayLabelColor="@color/colorAccent"
        />
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/calendar_helper_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/datePickCalendar"
        android:layout_above="@+id/backToChat"
        />

    <ImageButton
        android:id="@+id/btnEditCalendar"
        android:src="@drawable/ic_baseline_edit_24"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:background="@drawable/red_background_rectangle"
        android:clickable="true"
        android:focusable="true"
        android:textColor="#ffffff"
        android:layout_margin="10dp"
        />
    <ImageButton
        android:id="@+id/backToChat"
        android:src="@drawable/ic_baseline_arrow_back_24"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_alignParentEnd="false"
        android:layout_alignParentBottom="true"
        android:background="@drawable/red_background_rectangle"
        android:clickable="true"
        android:focusable="true"
        android:textColor="#ffffff"
        android:layout_margin="10dp"
        />

</RelativeLayout>